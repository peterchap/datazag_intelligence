# CLAUDE.md — datazag_intelligence

## System context (Datazag pipeline hub)

This repo is one component of the multi-server Datazag pipeline. The full system map,
cross-server flows, runbooks, and active workstreams live in the hub repo:
`C:\Code\datazag-pipeline` — read its `README.md` and `work/ACTIVE.md` before making
changes that touch other components, queues, Flight endpoints, or deployed servers.
If your work is part of a workstream in `work/`, read that doc first and update it
(status / state / next steps) + commit the hub before finishing.
